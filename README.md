Se crean varias clases. Artista que tendrá los atributos, el constructor, getters, setters y el método toString. Esto se repite 2 veces más para concierto y entrada. Conexión que tendrá la manera de conectar a la base de datos. Artista CRUD que tendrá los métodos para ejecutar la creación de objetos, su listado y su eliminación de la tabla. También se creará un CRUD para los otros dos.Y el Main donde se creará el menú y se ejecutarán los métodos de las 3 partes.
